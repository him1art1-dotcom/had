# دليل اختبار نظام حاضر (Hader System)

## 🚀 خطوات التشغيل

### 1. تثبيت المتطلبات
```bash
# تأكد من تثبيت Node.js (الإصدار 18 أو أحدث)
node --version

# تثبيت المكتبات المطلوبة
npm install
```

### 2. تشغيل المشروع
```bash
# تشغيل في وضع التطوير
npm run dev
```

سيتم فتح التطبيق على: `http://localhost:3000` (أو المنفذ المحدد في vite.config.ts)

---

## 🔐 بيانات تسجيل الدخول للاختبار

### الوضع المحلي (Local Mode - افتراضي)

#### 1. **المدير المسؤول (Site Admin)**
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`
- **الصلاحيات:** كاملة (تعطيل/تفعيل النظام، إرسال إعلانات، تحليل الأخطاء)

#### 2. **المراقب (Watcher)**
- **اسم المستخدم:** `watcher`
- **كلمة المرور:** `123`
- **الصلاحيات:** مراقبة الحضور اليومي، اعتماد التقارير

#### 3. **ولي الأمر (Guardian)**
- **رقم الجوال:** `501234567` (أو أي رقم موجود في بيانات الطلاب)
- **آخر 4 أرقام من معرف الطالب:** `4001` (معرف الطالب: `2024001`)

### الوضع السحابي (Cloud Mode)
- يتطلب إعداد Supabase أولاً
- استخدم بيانات المستخدمين من قاعدة البيانات السحابية

---

## 📋 سيناريوهات الاختبار

### ✅ 1. اختبار تسجيل الدخول

#### أ. تسجيل دخول المدير
1. افتح `http://localhost:3000`
2. أدخل `admin` / `admin123`
3. **المتوقع:** الانتقال إلى Dashboard الرئيسي

#### ب. تسجيل دخول المراقب
1. أدخل `watcher` / `123`
2. **المتوقع:** الانتقال إلى Dashboard مع خيارات محدودة

#### ج. تسجيل دخول ولي الأمر
1. اختر "ولي أمر"
2. أدخل رقم جوال + آخر 4 أرقام من معرف الطالب
3. **المتوقع:** الانتقال إلى بوابة أولياء الأمور

---

### ✅ 2. اختبار Dashboard (لوحة الإدارة)

#### أ. التحقق من البيانات الحقيقية
1. سجل دخول كـ `admin`
2. اذهب إلى `/admin` → تبويب "لوحة القيادة"
3. **تحقق من:**
   - إجمالي الطلاب (رقم حقيقي)
   - نسبة الحضور اليوم (حساب حقيقي)
   - حالات الغياب والتأخير (أرقام دقيقة)
   - الرسوم البيانية الأسبوعية (آخر 7 أيام)
   - أداء الصفوف (بيانات حقيقية)

#### ب. اختبار الرسوم البيانية
- **Area Chart:** يجب أن يعرض بيانات الأسبوع الماضي
- **Bar Chart:** يجب أن يعرض اتجاه آخر 30 يوم
- **Donut Chart:** يجب أن يعرض توزيع الحضور اليوم
- **Radar Chart:** يجب أن يعرض مؤشر الأداء الشامل

---

### ✅ 3. اختبار نظام الإشعارات

#### أ. اختبار الجرس في الشريط العلوي
1. سجل دخول كأي مستخدم (عدا الكشك)
2. **تحقق من:**
   - ظهور أيقونة الجرس أعلى الصفحة
   - ظهور عداد الإشعارات غير المقروءة (إذا وجدت)
   - فتح Popover عند النقر على الجرس
   - عرض قائمة الإشعارات

#### ب. اختبار Toast Notifications
1. من صفحة Support، أرسل إعلان مع تفعيل "منبّه فوري"
2. افتح صفحة أخرى (مثل Parents أو Dashboard)
3. **المتوقع:** ظهور Toast في الزاوية اليمنى السفلية

---

### ✅ 4. اختبار كشك الحضور (Kiosk)

#### أ. الوصول للكشك
1. سجل دخول كـ `admin` أو `watcher`
2. اذهب إلى `/kiosk`
3. **المتوقع:** شاشة كاملة لتسجيل الحضور

#### ب. تسجيل حضور
1. أدخل رقم طالب (مثل: `2024001`)
2. اضغط Enter
3. **المتوقع:**
   - رسالة نجاح/خطأ
   - صوت تنبيه (إذا متاح)
   - تحديث الإحصائيات

#### ج. اختبار شاشة التوقف
1. من صفحة Admin → "إعدادات الكشك"
2. فعّل "شاشة التوقف"
3. ارفع صور للشاشة
4. اذهب للكشك وانتظر timeout
5. **المتوقع:** ظهور شاشة التوقف مع الصور المرفوعة

#### د. اختبار التحكم عن بُعد
1. من صفحة Support، أرسل إعلان نوعه "أمر" للكشك
2. في نص الرسالة اكتب: `force screensaver`
3. **المتوقع:** تفعيل شاشة التوقف فوراً في الكشك

---

### ✅ 5. اختبار لوحة الدعم الفني (Support)

#### أ. الوصول للصفحة
1. سجل دخول كـ `admin` فقط
2. اذهب إلى `/support`
3. **المتوقع:** لوحة تحكم كاملة

#### ب. اختبار تعطيل/تفعيل النظام
1. في قسم "تحكم النظام"
2. اضغط على Toggle "حالة النظام"
3. **المتوقع:**
   - تغيير الحالة فوراً
   - رسالة توضيحية
   - حفظ في قاعدة البيانات

#### ج. اختبار لوحة تحليل الأخطاء
1. اضغط "فحص الآن"
2. **المتوقع:**
   - عرض نتائج الفحص
   - تصنيف (سليم/تحذير/خطأ)
   - اقتراحات الإصلاح

#### د. اختبار إرسال الإعلانات
1. في قسم "مركز البث"
2. اختر نوع الرسالة (إعلان/تنبيه/أمر)
3. اختر الجمهور المستهدف
4. أدخل العنوان والرسالة
5. فعّل "منبّه فوري" (اختياري)
6. اضغط "إرسال الرسالة"
7. **المتوقع:**
   - رسالة نجاح
   - وصول الإشعار للمستخدمين المستهدفين
   - ظهور Toast إذا كان isPopup

---

### ✅ 6. اختبار بوابة أولياء الأمور (Parents)

#### أ. الوصول للبوابة
1. سجل دخول كولي أمر
2. **المتوقع:** عرض قائمة الأبناء المرتبطين

#### ب. اختبار التبويبات
- **سجل الحضور:** عرض تاريخ الحضور
- **الاستئذان:** عرض سجلات الخروج
- **السلوك:** عرض المخالفات
- **الإشعارات:** عرض جميع الإشعارات

#### ج. اختبار Toast الفوري
1. من Support، أرسل إعلان لـ "أولياء الأمور" مع isPopup
2. **المتوقع:** ظهور Toast فوري في صفحة Parents

---

### ✅ 7. اختبار إدارة الطلاب (Admin → Students)

#### أ. إضافة طالب جديد
1. اذهب إلى `/admin` → "الطلاب"
2. اضغط "إضافة طالب"
3. املأ البيانات
4. **المتوقع:** حفظ الطالب وعرضه في القائمة

#### ب. استيراد من Excel
1. اضغط "استيراد"
2. اختر ملف Excel/CSV
3. **المتوقع:** معالجة الملف وإضافة الطلاب

---

### ✅ 8. اختبار إعدادات الكشك (Admin → Kiosk Settings)

#### أ. تعديل العناوين
1. اذهب إلى `/admin` → "إعدادات الكشك"
2. عدّل "العنوان الرئيسي" و "العنوان الفرعي"
3. احفظ
4. **المتوقع:** تحديث العناوين في الكشك

#### ب. رفع صور شاشة التوقف
1. اضغط "إضافة صورة لشاشة التوقف"
2. اختر صورة
3. **المتوقع:** تحويل للـ Base64 وحفظها
4. اذهب للكشك وتحقق من ظهور الصور

---

### ✅ 9. اختبار التقارير (Admin → Reports)

#### أ. إنشاء تقرير
1. اذهب إلى `/admin` → "التقارير"
2. اختر تاريخ من وإلى
3. اضغط "عرض التقرير"
4. **المتوقع:** عرض بيانات حقيقية

#### ب. تصدير التقرير
1. بعد إنشاء التقرير
2. اضغط "Excel" أو "PDF"
3. **المتوقع:** تحميل الملف

---

## 🔍 قائمة التحقق الشاملة

### الوظائف الأساسية
- [ ] تسجيل الدخول (جميع الأدوار)
- [ ] تسجيل الخروج
- [ ] التنقل بين الصفحات
- [ ] عرض Dashboard مع بيانات حقيقية

### نظام الإشعارات
- [ ] ظهور الجرس مع العداد
- [ ] فتح Popover الإشعارات
- [ ] Toast فوري عند isPopup
- [ ] إرسال إعلانات من Support

### كشك الحضور
- [ ] تسجيل حضور طالب
- [ ] عرض رسائل النجاح/الخطأ
- [ ] شاشة التوقف (timeout)
- [ ] شاشة التوقف (remote command)
- [ ] عرض الصور المرفوعة

### لوحة الإدارة
- [ ] Dashboard ببيانات حقيقية
- [ ] إدارة الطلاب (إضافة/استيراد)
- [ ] إدارة الصفوف
- [ ] إدارة المستخدمين
- [ ] إعدادات الكشك
- [ ] التقارير والتصدير

### لوحة الدعم الفني
- [ ] تعطيل/تفعيل النظام
- [ ] تعطيل/تفعيل المدرسة
- [ ] فحص الأخطاء
- [ ] إرسال إعلانات/تنبيهات/أوامر
- [ ] تغيير الثيمات
- [ ] عرض الإحصائيات

### بوابة أولياء الأمور
- [ ] عرض بيانات الأبناء
- [ ] سجل الحضور
- [ ] الاستئذان
- [ ] المخالفات
- [ ] الإشعارات
- [ ] Toast فوري

---

## 🐛 اختبار الأخطاء

### اختبار حالات الخطأ
1. **تسجيل حضور مكرر:** حاول تسجيل نفس الطالب مرتين في نفس اليوم
2. **طالب غير موجود:** أدخل رقم طالب غير موجود
3. **إشعار بدون عنوان:** حاول إرسال إعلان بدون عنوان
4. **اتصال Supabase:** إذا كان في وضع Cloud، اختبر انقطاع الاتصال

---

## 📱 اختبار Responsive Design

### اختبار على أحجام مختلفة
1. **Desktop:** 1920x1080
2. **Tablet:** 768x1024
3. **Mobile:** 375x667

**تحقق من:**
- ظهور جميع العناصر
- قابلية القراءة
- سهولة الاستخدام
- عدم وجود overflow

---

## ⚡ اختبار الأداء

### اختبار السرعة
1. **تحميل الصفحة:** يجب أن يكون < 2 ثانية
2. **الرسوم البيانية:** يجب أن تظهر بسرعة
3. **الإشعارات:** يجب أن تصل فوراً (< 1 ثانية)

---

## 🎨 اختبار التصميم

### التحقق من Glassmorphism
- [ ] جميع الكروت تستخدم glass-card
- [ ] تأثير blur واضح
- [ ] ألوان Neon متوهجة
- [ ] Animations سلسة

### التحقق من RTL
- [ ] جميع النصوص من اليمين لليسار
- [ ] الأيقونات في المكان الصحيح
- [ ] الجداول تبدأ من اليمين

---

## 🔄 اختبار Real-time Features

### اختبار التحديث الفوري
1. **فتح صفحتين:** افتح Dashboard في نافذتين
2. **سجل حضور من الكشك**
3. **المتوقع:** تحديث Dashboard فوراً في النافذة الأخرى

---

## 💾 اختبار التخزين

### اختبار Local Mode
1. تأكد أن النظام يعمل بدون Supabase
2. جميع البيانات تُحفظ في LocalStorage
3. البيانات تبقى بعد إعادة التحميل

### اختبار Cloud Mode
1. غيّر الوضع إلى Cloud (من Support)
2. تأكد من الاتصال بـ Supabase
3. البيانات تُحفظ في السحابة

---

## 📝 ملاحظات الاختبار

### سجل الملاحظات التالية:
- أي أخطاء واجهتها
- أي تحسينات مقترحة
- أي ميزات مفقودة
- أي مشاكل في الأداء

---

## 🚨 المشاكل الشائعة وحلولها

### المشكلة: الصفحة لا تحمل
**الحل:** تأكد من تشغيل `npm install` أولاً

### المشكلة: لا تظهر البيانات
**الحل:** تأكد من وجود بيانات تجريبية في LocalStorage

### المشكلة: الإشعارات لا تصل
**الحل:** تأكد من أن `subscribeToNotifications` يعمل بشكل صحيح

### المشكلة: الرسوم البيانية فارغة
**الحل:** تأكد من وجود بيانات حضور في الأيام السابقة

---

## ✅ بعد الاختبار

بعد إكمال جميع الاختبارات:
1. سجل جميع الأخطاء
2. راجع الأداء
3. تحقق من تجربة المستخدم
4. أعد الاختبار بعد الإصلاحات

---

**نصيحة:** ابدأ بالاختبارات الأساسية ثم انتقل للميزات المتقدمة.

**حظاً موفقاً في الاختبار! 🎉**

